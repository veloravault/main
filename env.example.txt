NEXT_PUBLIC_SUPABASE_URL=your_supabase_url_here
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=your_publishable_key_here
SUPABASE_SECRET_KEY=your_server_secret_key_here

# Legacy fallback only: NEXT_PUBLIC_SUPABASE_ANON_KEY. Remove after publishable-key verification.
NEXT_PUBLIC_SUPABASE_ANON_KEY=legacy_fallback_only
# Legacy fallback only: SUPABASE_SERVICE_ROLE_KEY. Remove after secret-key verification.
SUPABASE_SERVICE_ROLE_KEY=legacy_fallback_only

# Comma-separated immutable Supabase Auth user UUIDs. Start with the owner UUID only.
ADMIN_USER_IDS=your_owner_auth_uuid
# Exact application origin; no path, query, fragment, or trailing slash.
APP_URL=http://localhost:3000

GROQ_API_KEY=your_groq_api_key_here
GEMINI_API_KEY=your_gemini_api_key_here

# Cloudflare R2 - document blob storage (documents only; avatars stay on Supabase).
# Server-only: the browser never sees these. It uploads/downloads via short-lived
# presigned URLs minted by /api/storage/*. Endpoint is derived as
# https://<R2_ACCOUNT_ID>.r2.cloudflarestorage.com. Create an "Account" R2 API
# token (Object Read & Write) scoped to the bucket, and set bucket CORS to allow
# APP_URL + http://localhost:3000 for GET/PUT.
R2_ACCOUNT_ID=your_r2_account_id
R2_ACCESS_KEY_ID=your_r2_access_key_id
R2_SECRET_ACCESS_KEY=your_r2_secret_access_key
R2_DOCUMENTS_BUCKET=velora-documents

# Razorpay - recurring Plus subscriptions. Test mode: generate keys at
# Dashboard > Settings > API Keys (no KYC needed). RAZORPAY_KEY_ID is also sent
# to the client (it's a public identifier); RAZORPAY_KEY_SECRET never is. The
# 2 RAZORPAY_PLAN_* ids are created once via the API (see
# docs/superpowers/specs/2026-07-17-razorpay-subscriptions-design.md), not the
# dashboard. RAZORPAY_WEBHOOK_SECRET is a secret you invent when adding a
# webhook (Settings > Webhooks) pointing at APP_URL + /api/payments/webhook,
# subscribed to: subscription.activated, subscription.charged,
# subscription.pending, subscription.halted, subscription.cancelled,
# subscription.completed, payment.failed.
RAZORPAY_KEY_ID=your_razorpay_key_id
RAZORPAY_KEY_SECRET=your_razorpay_key_secret
RAZORPAY_WEBHOOK_SECRET=your_razorpay_webhook_secret
RAZORPAY_PLAN_PLUS_MONTHLY=plan_xxx
RAZORPAY_PLAN_PLUS_YEARLY=plan_xxx
